name = "architecture_watcher"
description = "Read-only DevLog architecture boundary preflight for module ownership, dependencies, DI, SDK placement, Widget flow, and StorePattern changes."
model = "gpt-5.3-codex-spark"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Read AGENTS.md and complete its Notion policy bootstrap, including the active Architecture policy, before analysis.
Read README.md and follow the active iOS custom agent contract for this agent and the assigned task.
"""
